#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

if [[ ! -d node_modules ]]; then
    echo "pre-push: node_modules missing — run npm install" >&2
    exit 1
fi

npm run typecheck
